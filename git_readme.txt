cd ruta/a/tu/carpeta
// clona git
git clone https://github.com/crissrocca/fullstack-course.git
cd fullstack-course

// (Opcional) Configura tu usuario y correo de Git
git config --global user.name "Tu Nombre"
git config --global user.email "tuemail@example.com"

// verificar que está conectado correctamente
git remote -v

/***
*** GIT
*/
git pulls
git status
git add .
git commit -m "comentario texto"
git push

# git checkout -b criss_
git push origin criss_:master


ERROR :
git pull origin master
i
esc
:wq